import React from 'react';
import ReactDOM from 'react-dom/client';
import FormApartment from './FormApartment';

const root = ReactDOM.createRoot(document.getElementById('form-mieszkanie'));
root.render(
  <React.StrictMode>
    <FormApartment />
  </React.StrictMode>
);
